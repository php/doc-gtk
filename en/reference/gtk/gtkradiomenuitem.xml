<?xml version="1.0" encoding="ISO-8859-1" ?>
<classentry id="gtk.gtkradiomenuitem">
 <classmeta>
  <classtitle>GtkRadioMenuItem</classtitle>
  <classparent>GtkCheckMenuItem</classparent>
  <shortdesc>
   Menu item that is part of a group, only one of which may be selected.
  </shortdesc>
  <desc>
   <simpara>
    A <classname>GtkRadioMenuItem</classname> is one of a group of similar
    widgets, only one of which may be selected at any one time.
   </simpara>
   <simpara>
    When the <classname>GtkMenuItem</classname> version of the 
    <signalname class="GtkMenuItem">activate</signalname> signal has been
    emitted by a <classname>GtkRadioMenuItem</classname>, there is a second
    emission as the most recently active item is toggled to 
    <literal>inactive</literal>.  This is dealt with before the original
    signal is handled.
   </simpara>
   <simpara>
    One way around this would be to use the 
    <signalname class="GtkWidget">button-press-event</signalname> signal. 
    Another would be to set up a count for each individual function and test
    that the count is not divisible by 2.  Or, depending on the kind of
    function you are calling, you could simply allow 
    <signalname class="GtkMenuItem">activate</signalname> to be emitted twice.
   </simpara>
  </desc>
 </classmeta>

 <constructor id="gtk.gtkradiomenuitem.constructor">
  <funcsynopsis>
   <funcprototype>
    <funcdef> <function>GtkRadioMenuItem</function></funcdef>
    <paramdef>
     <classname>GtkRadioMenuItem</classname> 
     <parameter>group</parameter>
    </paramdef>
    <paramdef>string <parameter>label</parameter></paramdef>
   </funcprototype>
  </funcsynopsis>
  <shortdesc>
   Creates one of a group of <classname>GtkRadioMenuItem</classname> widgets.
  </shortdesc>
  <desc>
   <simpara>
    The first <classname>GtkRadioMenuItem</classname> in a group should have
    the first parameter set to <literal>null</literal>.  This 'group leader' 
    is not attached to anything; there is not at present such a thing as a
    GtkGroup widget of any kind.
   </simpara>
   <simpara>
    All subsequent <classname>GtkRadioMenuItem</classname> widgets belonging
    to the same group should have the variable representing that group leader
    as the first parameter.
   </simpara>
   <note>
    <simpara>
     It is possible to mix different types of menu item within a 
     <classname>GtkMenu</classname>.
    </simpara>
   </note>
   <para>
    In the following code snippet, <literal>Open</literal> is the group leader
    and <literal>Save</literal> and <literal>Print</literal> the other options
    belonging to the group.  Note that <literal>Exit</literal> is not a part
    of that group, although it is part of the same menu.
    <informalexample>
     <programlisting role="php">
<![CDATA[
<?php

$menu = &amp;new GtkMenu();

$open = &amp;new GtkRadioMenuItem(null, &quot;Open&quot;);
$open-&gt;connect('button-press-event', 'open_routine');
$menu-&gt;append($open);

$save = &amp;new GtkRadioMenuItem($open, &quot;Save&quot;);
$save-&gt;connect('button-press-event', 'save_routine');
$menu-&gt;append($save);

$print = &amp;new GtkRadioMenuItem($open, &quot;Print&quot;);
$print-&gt;connect('button-press-event', 'print_routine');
$menu-&gt;append($print);

$separator = &amp;new GtkMenuItem();
$menu-&gt;append($separator);

$exit = &amp;new GtkRadioMenuItem(null, &quot;Exit&quot;);
$exit-&gt;connect_object('button-press-event','exit_routine', $menu);
$menu-&gt;append($exit);

?>
]]>
     </programlisting>
    </informalexample>
   </para>
  </desc>
 </constructor>

</classentry>
