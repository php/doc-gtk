<classentry id="gtk.gtkframe">
 <classmeta>
  <classtitle>GtkFrame</classtitle>
  <classparent>GtkBin</classparent>
  <shortdesc>
   Container with a decorative frame and an optional label.
  </shortdesc>
  <desc>
   <simpara>
    <classname>GtkFrame</classname> is a container for a single child
    widget that provides a configurable shadowed border around the child's
    allocated area.
   </simpara>
  </desc>
 </classmeta>

 <constructor id="gtk.gtkframe.constructor">
  <funcsynopsis>
   <funcprototype>
    <funcdef> <function>GtkFrame</function></funcdef>
    <paramdef>string 
     <parameter><optional>label = NULL</optional></parameter>
    </paramdef>
   </funcprototype>
  </funcsynopsis>
  <shortdesc>
   Creates a decorative container.
  </shortdesc>
  <desc>
   <simpara>
    The label in the <classname>GtkFrame</classname> is only present if
    the label's text is passed as the parameter during the construction of
    the widget.  Including a label will therefore expand the height of the
    area along the top of the frame to accommodate the internal 
    <classname>GtkLabel</classname> widget.  It is possible to pass the 
    <parameter>label</parameter> parameter as <literal>NULL</literal> in
    order to retain the height of that area.
   </simpara>
  </desc>
 </constructor>

 <methods>

  <method id="gtk.gtkframe.method.set_label">
   <funcsynopsis>
    <funcprototype>
     <funcdef>void <function>set_label</function></funcdef>
     <paramdef>string <parameter>label</parameter></paramdef>
    </funcprototype>
   </funcsynopsis>
   <shortdesc>
    Sets label text.
   </shortdesc>
   <desc>
    <simpara>
     <function class="GtkFrame">set_label()</function> sets the label
     text programmatically.  If you intend using this method, please
     bear in mind that you will need to create a <literal>NULL</literal> 
     <classname>GtkLabel</classname> at the outset in order to prevent the
     entire widget from redrawing and the label area from resizing.
    </simpara>
    <para>
     <informalexample>
      <programlisting role="php">
$frame = &amp;new GtkFrame(NULL);
      </programlisting>
     </informalexample>
    </para>
   </desc>
  </method>

  <method id="gtk.gtkframe.method.set_label_align">
   <funcsynopsis>
    <funcprototype>
     <funcdef>void <function>set_label_align</function></funcdef>
     <paramdef>double <parameter>xalign</parameter></paramdef>
     <paramdef>double <parameter>yalign</parameter></paramdef>
    </funcprototype>
   </funcsynopsis>
   <shortdesc>
    Sets label position.
   </shortdesc>
   <desc>
    <simpara>
     <function class="GtkFrame">set_label_align()</function> sets the
     position of the label along the top edge of the frame.  The position
     is given as a <literal>double</literal>, so that an 
     <parameter>xalign</parameter> setting of <literal>0.0</literal> would
     hold the label against the top left edge of the frame and a setting of
     <literal>1.0</literal> against the top right edge.  The default 
     <parameter>xalign</parameter> setting is <literal>0.0</literal>.
    </simpara>
    <simpara>
     Note that although the <parameter>yalign</parameter> setting is
     currently ignored by GTK, it is not possible to omit the parameter
     completely.  The default setting for <parameter>yalign</parameter> 
     is <literal>0.5</literal> both currently and in development releases
     of GTK+, so it would seem advisable to use that value.
    </simpara>
    <para>
     <informalexample>
      <programlisting role="php">
$frame-&gt;set_label_align(0.7, 0.5);
      </programlisting>
     </informalexample>
    </para>
   </desc>
  </method>

  <method id="gtk.gtkframe.method.set_shadow_type">
   <funcsynopsis>
    <funcprototype>
     <funcdef>void <function>set_shadow_type</function></funcdef>
     <paramdef>
      <enumname>GtkShadowType</enumname> <parameter>type</parameter>
     </paramdef>
    </funcprototype>
   </funcsynopsis>
   <shortdesc>
    Sets angle and depth of shadow.
   </shortdesc>
   <desc>
    <simpara>
     <function class="GtkFrame">set_shadow_type</function> sets the
     angle and depth of the line of shadow that decorates the 
     <classname>GtkFrame</classname> border, according to the specified
     <enumname>GtkShadowType</enumname> option.
    </simpara>
    <simpara>
     The default setting is <literal>GTK_SHADOW_ETCHED_IN</literal>.
    </simpara>
   </desc>
  </method>

 </methods>

</classentry>
