<classentry id="gtk.gtktearoffmenuitem">
 <classmeta>
  <classtitle>GtkTearoffMenuItem</classtitle>
  <classparent>GtkMenuItem</classparent>
  <shortdesc>
   Menu item used to tear off and reattach its menu.
  </shortdesc>
  <desc>
   <simpara>
    A <classname>GtkTearoffMenuItem</classname> is a dedicated type of 
    <classname>GtkMenuItem</classname> that can be prepended to a menu
    in order to give the user the option of tearing off the popped-up menu
    from its parent object (usually a menu item in a 
    <classname>GtkMenuBar</classname>), and keeping it within a non-resizeable
    modal <classname>GtkWindow</classname> of its own.
   </simpara>
   <simpara>
    The widget appears as a dotted line across the top of the menu when
    the menu is attached to a parent object, or as a dotted line with a
    left-pointing arrow where the menu is detached.  Clicking on the dotted
    line detaches an attached menu, and clicking on the arrow of a detached
    menu re-attaches it to the parent.
   </simpara>
  </desc>
 </classmeta>

 <constructor id="gtk.gtktearoffmenuitem.constructor">
  <funcsynopsis>
   <funcprototype>
    <funcdef> <function>GtkTearoffMenuItem</function></funcdef>
    <paramdef>void</paramdef>
   </funcprototype>
  </funcsynopsis>
  <shortdesc>
   Creates the means to tear off a menu.
  </shortdesc>
  <desc>
   <simpara>
    A <classname>GtkTearoffMenuItem</classname> will only work for a full
    menu, not for part of one.  It therefore needs to be prepended so that
    it takes the <literal>0</literal> position in the 
    <classname>GtkMenu</classname> where it is used:
   </simpara>
   <para>
    <informalexample>
     <programlisting role="php">
$menu = &amp;new GtkMenu();
$tearoff = &amp;new GtkTearoffMenuItem();
$menu-&gt;prepend($tearoff);
     </programlisting>
    </informalexample>
   </para>
  </desc>
 </constructor>

</classentry>
