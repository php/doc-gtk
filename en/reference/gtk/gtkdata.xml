<?xml version="1.0" encoding="ISO-8859-1" ?>
<classentry id="gtk.gtkdata">
 <classmeta>
  <classtitle>GtkData</classtitle>
  <classparent>GtkObject</classparent>
  <shortdesc>
   Abstract base class for objects containing data.
  </shortdesc>
  <desc>
   <simpara>
    <classname>GtkData</classname> is a legacy abstract base class providing
    inheriting objects with a single signal - which no longer operates.
   </simpara>
   <simpara>
    Objects that are descendants of this class are not widgets, and do not
    take up screen space.  They purely carry data.
   </simpara>
   <simpara>
    In GTK 2, <classname>GtkData</classname> will cease to exist entirely, 
    and the objects that currently are derived from it will be derived
    directly from <classname>GtkObject</classname> instead.
   </simpara>
  </desc>
 </classmeta>

 <signals>

  <signal id="gtk.gtkdata.signal.disconnect">
   <signalname>disconnect</signalname>
   <callback>
    <funcprototype>
     <funcdef>void <function>callback</function></funcdef>
     <paramdef><classname>GtkData</classname> object</paramdef>
    </funcprototype>
   </callback>
   <shortdesc>
    Never emitted.
   </shortdesc>
   <desc>
    <simpara>
     This is a legacy signal which has been implemented in 
     <classname>GtkData</classname> but nowhere else.  You won't get an error
     message by setting up a connection for it, but there is no longer any way
     to make an object emit it - so don't bother.
    </simpara>
   </desc>
  </signal>

 </signals>

</classentry>
