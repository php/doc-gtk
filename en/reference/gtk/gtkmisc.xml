<?xml version="1.0" encoding="ISO-8859-1" ?>
<classentry id="gtk.gtkmisc">
 <classmeta>
  <classtitle>GtkMisc</classtitle>
  <classparent>GtkWidget</classparent>
  <shortdesc>
   Base class for widgets having their own alignment and padding.
  </shortdesc>
  <desc>
   <simpara>
    <classname>GtkMisc</classname> is a base class bequeathing alignment
    and padding properties and related methods to its descendants.
   </simpara>
   <simpara>
    Most widgets do not have such properties on their own account, and
    only get them through being packed into a multiple container such as
    the <classname>GtkBox</classname> family members or 
    <classname>GtkTable</classname>, or else through using the 
    <classname>GtkAlignment</classname> widget which is designed to lend
    a facsimile of these properties to its child widget.
   </simpara>
   <simpara>
    It is therefore rare in GTK+ to find a widget capable of calling its
    own alignment and padding properties directly, and only 
    <classname>GtkMisc</classname> descendants are able to do so.
   </simpara>
   <simpara>
    This is an abstract base class, and cannot be directly constructed.
   </simpara>
  </desc>
 </classmeta>

 <methods>

  <method id="gtk.gtkmisc.method.set_alignment">
   <funcsynopsis>
    <funcprototype>
     <funcdef>void <function>set_alignment</function></funcdef>
     <paramdef>double <parameter>xalign</parameter></paramdef>
     <paramdef>double <parameter>yalign</parameter></paramdef>
    </funcprototype>
   </funcsynopsis>
   <shortdesc>
    Sets alignment.
   </shortdesc>
   <desc>
    <simpara>
     <function class="GtkMisc">set_alignment()</function> sets the
     relative positioning of the calling widget within its container. 
     <parameter>xalign</parameter> refers to the relative distance from
     the left edge, and <parameter>yalign</parameter> to the
     relative distance from the top edge of the container, with 
     <literal>0.0</literal> being no distance from those respective edges
     and <literal>1.0</literal> being no distance from their opposites.
    </simpara>
   </desc>
  </method>

  <method id="gtk.gtkmisc.method.set_padding">
   <funcsynopsis>
    <funcprototype>
     <funcdef>void <function>set_padding</function></funcdef>
     <paramdef>int <parameter>xpad</parameter></paramdef>
     <paramdef>int <parameter>ypad</parameter></paramdef>
    </funcprototype>
   </funcsynopsis>
   <shortdesc>
    Sets padding.
   </shortdesc>
   <desc>
    <simpara>
     <function class="GtkMisc">set_padding()</function> sets the padding
     around the calling widget to the given number of pixels, in both
     directions.
    </simpara>
   </desc>
  </method>

 </methods>

 <properties>

  <property id="gtk.gtkmisc.property.xalign" type="ro">
   <propname>xalign</propname>
   <proptype>double</proptype>
   <shortdesc>
    Relative distance from container's left edge.
   </shortdesc>
   <desc>
    <simpara>
     The <propname class="GtkMisc">xalign</propname> property provides
     the means to query the relative position of the widget to its
     container along the horizontal axis.
    </simpara>
   </desc>
  </property>

  <property id="gtk.gtkmisc.property.yalign" type="ro">
   <propname>yalign</propname>
   <proptype>double</proptype>
   <shortdesc>
    Relative distance from container's top edge.
   </shortdesc>
   <desc>
    <simpara>
     The <propname class="GtkMisc">yalign</propname> property provides
     the means to query the relative position of the widget to its
     container along the vertical axis.
    </simpara>
   </desc>
  </property>

  <property id="gtk.gtkmisc.property.xpad" type="ro">
   <propname>xpad</propname>
   <proptype>int</proptype>
   <shortdesc>
    Padding along horizontal axis.
   </shortdesc>
   <desc>
    <simpara>
     The <propname class="GtkMisc">xpad</propname> property provides the
     means to query the padding around the widget to the left and right of
     the area it takes up.  The returned integer represents pixels.
    </simpara>
   </desc>
  </property>

  <property id="gtk.gtkmisc.property.ypad" type="ro">
   <propname>ypad</propname>
   <proptype>int</proptype>
   <shortdesc>
    Padding along vertical axis.
   </shortdesc>
   <desc>
    <simpara>
     The <propname class="GtkMisc">ypad</propname> property provides the
     means to query the padding around the widget to the top and bottom of
     the area it takes up.  The returned integer represents pixels.
    </simpara>
   </desc>
  </property>

 </properties>

</classentry>
