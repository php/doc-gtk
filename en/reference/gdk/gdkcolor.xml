<?xml version="1.0" encoding="ISO-8859-1" ?>
<classentry id="gdk.gdkcolor">
 <classmeta>
  <classtitle>GdkColor</classtitle>
  <classparent></classparent>
  <shortdesc>
   A structure that holds a defined color.
  </shortdesc>
  <desc>
   <simpara>
    A <classname>GdkColor</classname> provides an object structure for
    retaining information about a defined color.
   </simpara>
   <simpara>
    This is an XcmsColor structure, and will take values in any XcmsColor
    format accordingly. Probably the most generally useful of these formats
    is the gamma-corrected XcmsRGB structure, which takes the range 
    <literal>0</literal> to <literal>65535</literal> per RGB value, with 
    <literal>0, 0, 0</literal> being the color black.  RGB values that are
    given in this way are scaled to match the color range of the hardware
    device currently being used. 
   </simpara>
   <simpara>
    Colors can be assigned through an application's RC files, where the 
    <classname>GdkColor</classname> objects are constructed via the 
    application colormap defined through the file.  Colors that are assigned
    in this way can be constructed using any of the formats accepted by the
    XcmsColor structure, including intensity doubles.
   </simpara>
   <simpara>
    Note that this is not the case when assigning a color to a 
    <classname>GdkColor</classname> directly.
   </simpara>
  </desc>
 </classmeta>

 <constructor id="gdk.gdkcolor.constructor">
  <funcsynopsis>
   <funcprototype>
    <funcdef> <function>GdkColor</function></funcdef>
    <paramdef>int <parameter>red</parameter></paramdef>
    <paramdef>int <parameter>green</parameter></paramdef>
    <paramdef>int <parameter>blue</parameter></paramdef>
   </funcprototype>
  </funcsynopsis>
  <shortdesc>
   Creates a new color object using one of three possible constructs.
   <!-- 
    I've no way to express alternative constructors .. have I? - sf mar 02 
   -->
  </shortdesc>
  <desc>
   <simpara>
    When constructing a <classname>GdkColor</classname> directly, you will
    need to use the system's color names, hex RGB triples or XcmsRGB values. 
    Attempting to use other formats here will not result in an error message,
    because the structure itself recognises them, but equally they will not
    display, because the function doesn't recognise them at all.
   </simpara>
   <para>
    The options when directly constructing colors in PHP-GTK are:
    <informalexample>
     <programlisting role="php">
/* XcmsRGB values, expressed as three integers */
$cyan = &amp;new GdkColor(0, 65535, 65535);
     </programlisting>
    </informalexample>
    or
    <informalexample>
     <programlisting role="php">
/* hex triples expressed as a single string */
$cyan = &amp;new GdkColor('#00FFFF');
     </programlisting>
    </informalexample>
    or
    <informalexample>
     <programlisting role="php">
/* a color name expressed as a single string */
$cyan = &amp;new GdkColor('cyan');
     </programlisting>
    </informalexample>
    Color names that are used on a given system are defined in a file
    named <filename>rgb.txt</filename>.  The equivalent file is not
    available in human-readable form under win32, although utilised. 
    <ulink url="&url.rgbtxt;">Here</ulink> is one of the better reference
    pages currently online detailing the standard color names used by
    window management systems.
   </para>
   <simpara>
    See also: <classname>GtkStyle</classname>.
   </simpara>
   <!-- 
    TODO We definitely, definitely need something in the userguide on
    RC files, soonest - sf mar 02
   -->
  </desc>
 </constructor>

</classentry>
