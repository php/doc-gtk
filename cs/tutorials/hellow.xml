<?xml version="1.0" encoding="ISO-8859-2" ?>
<chapter id="tutorials.hellow">
 <title>Hello World</title>
 <simpara>
  První program, který napíšete v novém programovacím jazyce, obvykle bývá
  klasické 'hello world'. Abychom neporušovali tradice, prvním příkladem v
  tomto manuálu bude 'hello world'.
 </simpara>
 <simpara>
  V příkladech předpokládáme jisté znalosti PHP samotného. Příklady jsou
  navrženy tak, aby uživatel získal přehled o použití PHP-GTK a
  prostředcích, které přináší.
 </simpara>
 <simpara>
  V prvním prográmku vytvoříme jednoduché okno s textem "Ahoj, světe!".
 </simpara>
 <simpara>
  Nejprve se podíváme, jak takový program bude vypadat, a poté vysvětlíme
  každý řádek programu.
 </simpara>
 <para>
  <example>
   <title>Výpis programu Hello World v PHP-GTK</title>
   <programlisting role="php">
if (!class_exists('gtk')) {
    if (strtoupper(substr(PHP_OS, 0,3) == 'WIN'))
    dl('php_gtk.dll');
  else
    dl('php_gtk.so');
}
    
function delete_event()
{
    return false;
}
    
function vypni()
{
    print(&quot;Vypínám...\n&quot;);
    gtk::main_quit();
}
    
function ahoj()
{
    global $okno;
    print &quot;Ahoj, světe!\n&quot;;
    $okno-&gt;destroy();
}
    
$okno = &amp;new GtkWindow();
$okno-&gt;connect('destroy', 'vypni');
$okno-&gt;connect('delete-event', 'delete_event');
$okno-&gt;set_border_width(10);
    
$tlacitko = &amp;new GtkButton('Ahoj, světe!');
$tlacitko-&gt;connect('clicked', 'ahoj');
$okno-&gt;add($tlacitko);
    
$okno-&gt;show_all();
    
gtk::main();
   </programlisting>
  </example>
 </para>
 <para>
  <example>
   <title>Určení PHP-GTK</title>
   <programlisting role="php">
if (!class_exists('gtk')) {
    if (strtoupper(substr(PHP_OS, 0,3) == 'WIN'))
    dl('php_gtk.dll');
 else
    dl('php_gtk.so');
}
   </programlisting>
  </example>
  Těchto pár řádků zkontroluje, zda už je PHP-GTK rozšíření dostupné. A
  pokud ne, nahraje ho do paměti. To zařídí příkaz
  <literal>dl('php_gtk.dll');</literal> pro Windows nebo
  <literal>dl('php_gtk.so');</literal> pro Linux.
 </para>
 <para>
  <example>
   <title>Funkce delete_event()</title>
   <programlisting role="php">
function delete_event()
{
    return false;
}
   </programlisting>
  </example>
  Funkce <literal>delete_event()</literal> je ovladač (viz níže) signálu
  <signalname>delete-event</signalname>. Vrátí hodnotu
  <literal>false</literal>, čímž oznámí PHP-GTK, aby použilo standardní
  ovladač události, což je v tomto případě metoda
  <function class="GtkObject">destroy()</function>. Pokud by funkce vrátila
  hodnotu <literal>true</literal>, PHP-GTK by zastavilo provádění
  standardního ovladače. To je užitečné, pokud chcete použít vlastní funkci
  namísto <function class="GtkObject">destroy()</function> - například k
  vytvoření dialogového okna, kde uživatel potvrdí, že chce opravdu aplikaci
  ukončit.
 </para>
 <simpara>
  Není vyloženě nutné vracet hodnotu <literal>false</literal>, aby se signál
  <signalname>delete-event</signalname> propojil s metodou
  <function class="GtkObject">destroy()</function>, protože tento signál
  standardně vrací hodnotu <literal>false</literal>. Není tedy vůbec nutné
  určovat chování pro signál <signalname>delete-event</signalname>, pokud je
  signál <signalname>destroy</signalname> správně obsloužen.
 </simpara>
 <para>
  <example>
   <title>Funkce vypni()</title>
   <programlisting role="php">
function shutdown()
{
    print(&quot;Vypínám...\n&quot;);
    gtk::main_quit();
}
   </programlisting>
  </example>
  Funkce <literal>vypni()</literal> je ovladač signálu
  <signalname>destroy</signalname>. Funkce vypíše na konzoli text
  <literal>&quot;Vypínám...\n&quot;</literal> a poté zavolá statickou funkci
  <function>gtk::main_quit()</function>.
 </para>
 <para>
  <example>
   <title>Funkce ahoj()</title>
   <programlisting role="php">
function ahoj()
{
    global $okno;
    print &quot;Ahoj, světe!\n&quot;;
    $okno-&gt;destroy();
}
   </programlisting>
  </example>
  Funkce <literal>ahoj()</literal> je ovladač signálu
  <signalname>clicked</signalname> tlačítka. Nejprve určí globální proměnnou
  <literal>$okno</literal>, aby bylo možno přistupovat k instanci objektu
  <classname>GtkWindow</classname> vytvořené dále ve skriptu. Poté vypíše na
  konzoli text <literal>&quot;Ahoj, světe!&quot;</literal> a nakonec zavolá
  metodu <function class="GtkObject">destroy()</function> na okno, což
  vyvolá signál <signalname>destroy</signalname>, který spustí funkci
  <literal>shutdown()</literal>.
 </para>
 <simpara>
  Další možností, jak může funkce <literal>ahoj()</literal> přistupovat k
  proměnné <literal>$okno</literal>, je její předání jako
  <link linkend="signals.custom.params">parametru</link>.
 </simpara>
 <para>
  <example>
   <title>Nastavení okna</title>
   <programlisting role="php">
$okno = &amp;new GtkWindow();
$okno-&gt;connect('destroy', 'vypni');
$okno-&gt;connect('delete-event', 'delete_event');
$okno-&gt;set_border_width(10);
   </programlisting>
  </example>
  Další čtyři řádky nastavují samotné okno. Nejprve vytvoříme instanci
  <classname>GtkWindow</classname>. Poté dvakrát zavoláme metodu 
  <function class="GtkObject">connect()</function>, abychom zaregistrovali
  funkci <literal>vypni()</literal> jako ovladač signálu
  <signalname>destroy</signalname> a funkci
  <literal>delete_event()</literal> jako ovladač signálu
  <signalname>delete-event</signalname>. Nakonec zavoláme funkci
  <function class="GtkContainer">set_border_width()</function>, kterou
  nastavíme práve vytvořené instanci <classname>GtkWindow</classname> 10
  pixelů široký okraj.
 </para>
 <para>
  <example>
   <title>Nastavení tlačítka</title>
   <programlisting role="php">      
$tlacitko = &amp;new GtkButton('Ahoj, světe!');
$tlacitko-&gt;connect('clicked', 'ahoj');
$ökno -&gt;add($tlacitko);

$okno-&gt;show_all();
   </programlisting>
  </example>
  Další tři řádky skriptu vytvoří a nastaví tlačítko. Na prvním řádku
  vytvoříme novou instanci widgetu <classname>GtkButton</classname>.
  Parametrem konstruktoru je text, který chceme zobrazit na tlačítku - v
  našem případě <literal>&quot;Ahoj, světe!&quot;</literal>. Následně
  zavoláme metodu <function class="GtkObject">connect()</function> a
  zaregistrujeme výše definovanou funkci <literal>ahoj()</literal> jako
  ovladač signálu <signalname>clicked</signalname>. Nakonec přidáme tlačítko
  do dříve vytvořeného okna. Použijeme metodu
  <function class="GtkContainer">add()</function> z instance
  <literal>$okno</literal>. V posledním (čtvrtém) řádku zavoláme metodu
  <function class="GtkWidget">show_all()</function> instance
  <literal>$okno</literal> a tím se zobrazí všechny widgety, které daný
  objekt obsahuje (v našem případě okno samotné a tlačítko).
 </para>
 <para>
  <example>
   <title>Zavoláme gtk::main</title>
   <programlisting role="php">
gtk::main();
   </programlisting>
  </example>
  Poslední řádek skriptu volá statickou funkci
  gtk::<function>main()</function>. Tak oznámíme PHP-GTK, že jsme skončili s
  nastavováním prostředí a je možno spustit hlavní smyčku, která bude
  čekat na události spuštěné uživatelem a v návaznosti na ně provádět
  námi definované funkce.
 </para>
</chapter>

<!--

 * Local variables:
 * tab-width: 1
 * c-basic-offset: 1
 * End:
 * vim600: et sw=1 ts=1 tw=78 fdm=marker
 * vim<600: et sw=1 ts=1 tw=78
-->

